# Managed by deploy/dotfiles/bootstrap.sh — re-running the bootstrap
# overwrites this file, so make edits upstream in the repo, not here.
#
# tmux itself is already the stock apt package on kwanz-ws; this is just
# the small amount of config worth having on a box people SSH into and
# leave sessions running on across disconnects.

set -g mouse on
set -g history-limit 50000
